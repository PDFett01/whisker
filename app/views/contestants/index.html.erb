<p id="notice"><%= notice %></p>

<h1>Registered Competitors</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Club</th>
      <th>City</th>
      <th>Style</th>
      <th>Competitions</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @contestants.each do |contestant| %>
      <tr>
        <td><%= contestant.name %></td>
        <td><%= contestant.club %></td>
        <td><%= contestant.city %></td>
        <td><%= contestant.style %></td>
        <td><%= contestant.competitions %></td>
        <td><%= link_to 'Show', contestant %></td>
        <td><%= link_to 'Edit', edit_contestant_path(contestant) %></td>
        <td><%= link_to 'Destroy', contestant, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Contestant', new_contestant_path %>
